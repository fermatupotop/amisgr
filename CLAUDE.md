# АМИС ГРУПП — дочерняя тема Astra

Сайт amisgr.ru: ООО «АМИС ГРУПП», поставщик радиоизмерительного оборудования
(RIGOL, Siglent, Keysight, Tektronix, АКИП, ПриСТ). Дочерняя тема Astra,
WooCommerce, без ACF/Elementor — страницы пишутся вручную на PHP.
Подробности установки и настройки атрибутов — в `README.md`.

## Конвенции, которые нужно соблюдать

**Как делать новую страницу — два способа, оба уже используются:**
- `page-{slug}.php` — WordPress находит по адресу страницы сам, без выбора
  в админке. Годится только для страниц с зафиксированным навсегда адресом
  (`page-contact.php`, `page-payment.php`, `page-documents.php` — их URL уже
  зашит в хедере/футере).
- `templates/template-*.php` с `Template Name:` в шапке файла — привязка
  через явный выбор шаблона в редакторе страницы, не ломается при смене slug.
  **Это предпочтительный способ для новых страниц**, если нет причины жёстко
  привязываться к URL. Так сделаны `template-home.php` и `template-about.php`.

**Экранирование вывода всегда**: `esc_html()`, `esc_url()`, `esc_attr()`,
`wp_kses_post()` для HTML. Если экранирование пропущено — рядом комментарий,
почему это безопасно (например, захардкоженный SVG-путь, не пользовательский ввод).

**Данные компании — только из `inc/company.php`**: реквизиты
(`amis_company_details()`), менеджеры (`amis_company_managers()`), телефоны
(`amis_company_phones()`, `amis_phone_link()`), почта (`amis_company_email()`).
Не дублировать эти значения текстом в шаблонах.

**Стили грузятся адресно** (`functions.php` → `amis_enqueue_assets`):
`base.css` — везде, `page.css` — на всех обычных страницах кроме главной,
`home.css` — только на `template-home.php`, `product.css` — только на карточке товара.
Новый CSS для очередной обычной страницы дописывается в `assets/css/page.css`,
а не в новый файл — так уже работают контакты, доставка/оплата и «О компании».

## Грабли с Astra, на которые уже наступали

**Новый `templates/template-*.php` обязательно нужно завести в
`amis_page_builder_layout()` (`inc/enqueue.php`, не в functions.php — этот
блок туда вынесен)**, иначе Astra оборачивает страницу в свой контейнер и
добавляет собственные отступы поверх наших — итог непредсказуемый
(например, «пропадающий» отступ у секции). Сейчас это сделано универсально:
любой шаблон, у которого slug начинается с `templates/template-`, получает
`page-builder` автоматически. Для `page-contact.php`, `page-payment.php` и
`page-documents.php` — то же самое, но по имени файла, явными строками
(у них нет общего префикса, как у `templates/*`).

**Заголовки (`h1`–`h4`) нельзя красить только на родителе.** Если задать
`color` на секции и рассчитывать, что `<h1>` внутри унаследует его —
не сработает: у Astra почти наверняка есть собственное правило вида
`h1{color:...}`, а любое ПРЯМОЕ правило на элементе побеждает
унаследованное значение независимо от специфичности. Красить нужно
явно через селектор, доходящий до самого заголовка
(`.section--dark h1{color:#fff}`), не полагаясь на наследование.

**На `:hover` у любой кнопки — `<a>` или `<button>` — всегда указывать
`color` явно, даже если он не меняется.** У Astra/WooCommerce есть глобальные
hover-правила и для ссылок (`a:hover{color:...}`), и для `.woocommerce
button.button`/`.woocommerce a.button` — оба по специфичности перебивают
наш класс кнопки именно в момент ховера, если мы сами не указали `color`
в своём hover-правиле. Из-за этого белый текст на кнопке на ховере
откатывался к тёмному цвету Astra. Уже поправлено для `.btn-primary`,
`.btn-dark`, `.btn-ghost`, `.catbtn`, `.buy` (все — `<a>`) и для
`button.single_add_to_cart_button` на карточке товара (`<button>` —
несмотря на более раннее предположение, что `<button>` эта проблема не
касается, WooCommerce вешает конфликтующее hover-правило и на него тоже).
Если добавляете новую кнопку с hover-эффектом — сразу пишите `color` в
правиле `:hover`, не полагайтесь на наследование от базового класса,
независимо от тега.

**Центрирование иконок в `.c-ico` — через `position:absolute` + `transform`,
не через `display:grid/flex`.** Тоже по вине Astra: где-то в её стилях
`display` на дочерних элементах контента переопределяется, и
`place-items:center`/`align-items:center` просто переставали работать.
`position:relative` на родителе почти никогда не трогается чужим CSS,
поэтому центрирование через абсолютное позиционирование надёжнее.

## Дизайн-токены (assets/css/base.css)

```
--orange:#D9673C; --orange-deep:#B44E28; --orange-soft:#FCF0EA;
--ink:#232629; --graphite:#3E4247; --gray:#53565A; --gray-2:#767A7F;
--muted:#9CA1A6; --line:#E2E4E6; --line-2:#EDEEF0; --panel:#F3F4F5;
--green:#3E8E5A; --r:3px; --wrap:1360px; --wrap-wide:1600px;
```
Шрифты: `IBM Plex Sans` (текст), `IBM Plex Mono` (цифры, `.num`).
Готовые компоненты для переиспользования: `.section`/`.s-head`/`.eyebrow`
(шапка страницы), `.btn`/`.btn-primary`/`.btn-ghost`/`.btn-dark` (кнопки),
`.c-card`/`.d-card`/`.d-doc` (карточки с иконкой), `.manager`/`.managers`
(карточки сотрудников), `.details` (таблица реквизитов). Иконки — инлайн SVG,
`stroke="currentColor"`, без иконочного шрифта.

## Текущее состояние страницы «О компании»

- Шаблон: `templates/template-about.php` (Template Name «О компании»).
- Стили: добавлены в конец `assets/css/page.css`.
- В `page-contact.php` у блока реквизитов есть якорь `id="details"` —
  на него ссылается страница «О компании».
- В `footer.php` кнопка «О компании» в блоке `.f-actions` ссылается на
  `/about/` — **это предположение об адресе страницы**. Когда страница будет
  создана в админке, проверить реальный slug и поправить ссылку, если он другой.
- Пункт «О компании» в верхнем меню — это запись в базе (Внешний вид → Меню),
  правится только в админке, не в коде.

## Текущее состояние страницы «Вакансии»

- Шаблон: `templates/template-vacancies.php` (Template Name «Вакансии»).
- Открытых вакансий пока нет: массив `$vacancies` в начале файла пустой —
  когда появятся реальные позиции, заполнить его, сетка карточек и кнопки
  «Откликнуться» отрисуются сами, менять разметку не нужно.
- Отклик пока идёт на `mailto:` (`amis_company_email()`) с темой «Резюме —
  вакансия» — отдельной формы или адреса для HR нет.
- Ссылка в `footer.php`, колонка «Информация» → группа «Помощь», ведёт на
  `/vacancies/` — **предположение об адресе**, проверить после создания
  страницы в админке (тот же нюанс, что и с `/about/`, см. выше).
- В верхнее меню пункт не добавлен — по решению пользователя, только футер.

## Текущее состояние страницы «Гарантия и сервис»

- Шаблон: `templates/template-warranty.php` (Template Name «Гарантия и сервис»).
- Ссылка уже была в `footer.php` («Гарантия и возврат» → `/warranty/`) —
  ничего добавлять не пришлось, страница просто должна получить этот slug.
- Подтверждённые пользователем условия (не выдумка, использовать как есть):
  гарантия 3 года на основное оборудование, 1 год на аксессуары/принадлежности,
  бессрочный платный постгарантийный сервис; формулировка обязательств —
  «гарантийный сервис по всем брендам каталога» (RIGOL, Siglent, Keysight,
  Tektronix, АКИП, ПриСТ), без привязки к одному производителю; сервисный
  центр — тот же адрес, что офис и склад (Алтуфьевское шоссе, 48к1).
- Срок ремонта нигде не назван точным числом дней (не подтверждён) —
  формулировка «сообщаем срок сразу после диагностики». Если появится
  реальная цифра — можно смело вписать в шаг «Диагностика» в шаблоне.

## Текущее состояние страницы «Политика конфиденциальности»

- Шаблон: `templates/template-privacy.php` (Template Name «Политика
  конфиденциальности»). Ссылка `/privacy/` уже была в футере.
- Это стандартный черновик на основе 152-ФЗ, не юридическая консультация —
  в шаблоне об этом есть комментарий. Если появятся SMS/email-рассылки,
  платёжный агрегатор на самом Сайте (не просто безнал по счёту) или
  сторонняя аналитика (Метрика/GA) — тексту нужны отдельные пункты,
  сейчас их нет, потому что неизвестно, используются ли эти инструменты.
- Реквизиты и почта берутся из `inc/company.php` (`amis_company_details()`,
  `amis_company_email()`), текстом не задублированы.

## Текущее состояние страницы «Вопросы и ответы» (FAQ)

- Шаблон: `templates/template-faq.php` (Template Name «Вопросы и ответы»).
- Аккордеон на нативных `<details>/<summary>`, без JS. Вопросы сгруппированы
  в массиве `$faq_groups` по темам (заказ и оплата, доставка, гарантия,
  демо/аренда) — добавлять новые вопросы можно прямо в массив,
  разметка отрисуется сама.
- Ссылка `/faq/` уже была в `footer.php` — ничего добавлять не пришлось.

## Уведомление о cookie

- Разметка в `footer.php` (выводится на всех страницах), стили и логика
  показа — в `base.css`/`base.js`. Показывается один раз, пока в
  `localStorage` нет отметки `amis_cookie_ack`; по нажатию «Понятно» больше
  не появляется на этом устройстве.
- Юридически необязателен для РФ (152-ФЗ не требует cookie-consent как
  GDPR) — добавлен как жест доверия. Если появится рекламный трекинг с
  передачей данных третьим лицам или сайт станет обслуживать посетителей
  из ЕС — баннер нужно переделать под явный opt-in.

## Текущее состояние страницы «Поверка и калибровка»

- Шаблон: `templates/template-verification.php` (Template Name «Поверка и
  калибровка»). Ссылки `/verification/` уже были в `header.php` и `footer.php`.
- Подтверждённые пользователем условия (не выдумка, использовать как есть):
  поверку выполняет **аккредитованный ЦСМ-партнёр, а не мы сами** — мы
  организуем, возим и ведём документы; первичная поверка входит в стоимость
  поставки; типовой срок 5–10 рабочих дней; результат — запись во ФГИС
  «Аршин», бумажное свидетельство и знак поверки; на поверку принимаем
  **только оборудование, поставленное нами**.
- **Чего на странице сознательно нет**: собственной аккредитации на поверку,
  вывоза и возврата прибора нашими силами, состава документов и стоимости
  калибровки (калибровка описана как организуемая по запросу).
- **Из карточки услуг на главной убрано «Ведём график МПИ по вашему парку»** —
  пользователь подтвердил, что график МПИ мы не ведём. Если услуга появится,
  вернуть текст и на главную (`templates/template-home.php`, массив
  `$services`), и отдельным блоком на страницу поверки.
- Адрес приёма берётся из `amis_company_details()['Фактический адрес']`,
  текстом не задублирован. В `page.css` добавлены `.cover-card p` и
  `.cover-card p + .cover-list` — раньше в этих карточках был только список.

## Текущее состояние страницы «Аренда приборов»

- Шаблон: `templates/template-rent.php` (Template Name «Аренда приборов»).
  Ссылки `/rent/` уже были в `header.php` и `footer.php` — ничего добавлять
  не пришлось, страница должна получить именно этот slug.
- Подтверждённые пользователем условия (не выдумка, использовать как есть):
  минимальный срок аренды — неделя; арендные платежи засчитываются при
  выкупе прибора; ставка считается индивидуально, никаких цифр и процентов
  на странице нет; залог не требуется, обеспечение — сам договор; аренда
  только юрлицам и ИП; в стоимость входят доставка до объекта и обратно
  и комплект аксессуаров.
- **Чего на странице сознательно нет**, потому что не подтверждено: поверка
  внутри арендной ставки (вынесена в «остаётся на вашей стороне»), подменный
  прибор на время ремонта, аренда физлицам, конкретные ставки. Появятся
  условия — дописать в готовую разметку.
- Своего CSS у страницы нет: переиспользованы `.warranty-grid`/`.warranty-card`,
  `.cover-grid`/`.cover-list`, `.why-grid`, `.steps`, `.d-note`, `.d-cta`.
  В `page.css` добавлены только `.d-note a` / `.d-note a:hover` — раньше
  внутри этой плашки ссылок не было.

## Текущее состояние страницы «Уставные документы»

- Файл: `page-documents.php` (фиксированный адрес `/documents/`, как
  `page-contact.php`/`page-payment.php`) — заведён в `amis_page_builder_layout()`
  в `inc/enqueue.php`.
- Список документов (`$documents` в начале файла) и ссылки в `/documents/`
  (`ustav.pdf`, `ogrn.pdf`, `inn.pdf`, `egrul.pdf`, `reshenie.pdf`,
  `prikaz-direktor.pdf`) — **заглушки вёрстки**, файлов на сервере нет.
  Как только появятся сканы — положить их в `/documents/` под этими же
  именами (или поправить пути в массиве), разметку менять не нужно.
- Секция реквизитов — по образцу `template-about.php`: сокращённый набор
  полей + ссылка «Все реквизиты» на `/contact/#details`, полный список не
  дублируется.

## Текущее состояние «Базы знаний»

- В отличие от остальных страниц — это не статический шаблон, а настоящий
  блог на записях WP (post), потому что контент сюда будет добавляться
  постоянно и без участия разработчика. Три файла:
  - `templates/template-articles.php` (Template Name «База знаний») —
    архив: любая опубликованная запись появляется здесь сама, привязки к
    отдельной категории нет. Фильтр по категориям строится из реально
    существующих категорий (`get_categories()`) и не показывается, пока
    категория одна. Пагинация — `/articles/page/2/` через query var `page`
    (у статических страниц это не `paged`, как у архивов, — уже учтено).
  - `single.php` (в корне темы) — WordPress сам подставляет его для любой
    записи, в админке ничего выбирать не нужно. Показывает похожие статьи
    (та же категория, если задана).
  - `template-parts/article-card.php` — карточка, общая для архива и блока
    «Похожие статьи»; ожидает, что уже отработал `the_post()` в цикле.
  - Стили — новый `assets/css/article.css` (не `page.css` — это новый тип
    контента, а не очередная обычная страница), подключается в
    `inc/enqueue.php` на `is_singular('post')` и на шаблоне архива вместе с
    `page.css` (нужны `.eyebrow`, `.section--head`, `.crumbs` и так далее).
  - `amis_page_builder_layout()` и переименованный `amis_no_sidebar_layout()`
    (был `amis_home_page_layout()`) в `inc/enqueue.php` — убирают контейнер
    и сайдбар Astra на записи и на архиве, как у остальных своих шаблонов.
- Ссылка `/articles/` уже была в `footer.php` — создать страницу в админке
  и выбрать шаблон «База знаний», записи публикуются штатно через
  Записи → Добавить новую, категории — через Записи → Рубрики.
- Обложка статьи — `has_post_thumbnail()`/`the_post_thumbnail()` (штатная
  миниатюра записи); без неё карточка и шапка статьи используют плейсхолдер
  без картинки — вёрстка не ломается.
- Что писать: практические статьи, которые помогают выбрать и понять
  прибор, — «Как выбрать осциллограф по полосе пропускания», разбор
  терминов и стандартов, разница «поверка vs калибровка», сравнения
  моделей/брендов, типовые задачи (лаборатория, КБ, производство). Не
  дублировать FAQ (короткие ответы уже на `/faq/`) и не пересказывать
  описания товаров с карточек.

## Текущее состояние архива каталога (/shop/, категории, метки)

- До этого архив вообще не имел своих стилей — `product.css` грузится
  только на карточке товара (`is_product()`), а `page.css`/`home.css` не
  подхватывали `is_shop()`/`is_product_taxonomy()`. В итоге WooCommerce/Astra
  рисовали архив полностью голым (список в один ряд без карточек).
  Поправлено: `woocommerce/archive-product.php` — свой шаблон архива,
  заменяет цикл WooCommerce целиком.
- Карточка — та же `template-parts/product-card.php`, что и в блоке «Хиты
  продаж» на главной (`templates/template-home.php`) — разметку/классы
  менять не нужно, если понадобится третье место с такой же сеткой.
- Стили — новый `assets/css/shop.css` (не `page.css`: это новый тип
  контента — сетка карточек товара, а не очередная обычная страница, тот
  же принцип, что и у `article.css` для «Базы знаний»). Подключается вместе
  с `page.css` в `amis_enqueue_assets()` (`inc/enqueue.php`), там же —
  `is_shop() || is_product_taxonomy()` заведено в `amis_page_builder_layout()`
  и `amis_no_sidebar_layout()`, иначе Astra добавляет свой контейнер/сайдбар
  поверх (см. «Грабли с Astra» выше).
- Фильтр по категориям вверху архива — те же топ-категории, что и в
  мега-меню каталога (`amis_get_top_categories()`, `inc/queries.php`),
  включая уже настроенное исключение служебной категории «Misc».
- Сортировки/пагинации через AJAX нет — только постраничная навигация
  (`paginate_links()`), как на «Базе знаний». Осознанно: сайт B2B,
  предполагается, что покупатель либо ищет через поиск/фильтр категорий,
  либо связывается с менеджером, а не перебирает сортировки каталога.

## Текущее состояние страницы «Корзина»

- В отличие от архива/карточки товара — здесь **нет** своего
  `woocommerce/cart/cart.php`. Шаблон WooCommerce сознательно не
  переопределён: там штатная логика купонов, кросс-продаж, nonce и
  пересчёта сумм, которую легко тонко сломать, переписывая по памяти.
  Вместо этого `inc/cart.php` оборачивает то, что WooCommerce и так
  выводит, через хуки `woocommerce_before_cart` / `woocommerce_after_cart` —
  они срабатывают в начале и в конце шаблона независимо от того, пуста
  корзина или нет, поэтому обёртка (`.wrap > .cart-page`) не «поедет» ни
  в одном из состояний.
- Там же, в `inc/cart.php`, — свои крошки и `<h1>` (по образцу остальных
  страниц, `.section--head`), а штатный заголовок Astra на странице
  «Корзина» отключён фильтром `astra_the_title_enabled` (тем самым, что
  упомянут в README) — иначе был бы дубль.
- Оформление — `assets/css/cart.css`, по классам, которые отдаёт сама
  WooCommerce (`.woocommerce-cart-form`, `.cart-collaterals`, `.cart_totals`,
  `.checkout-button` и так далее), а не по своим — эти классы стабильны и
  не меняются годами, в отличие от внутренней структуры шаблона.
- Кнопки количества (+/-) рисует сама Astra (её собственный
  `woocommerce.js`) — в теме для этого ничего не заводили, `cart.css`
  только оформляет уже существующие `.plus`/`.minus`/`.qty`. Если после
  обновления Astra эти классы изменятся и стрелки визуально разъедутся —
  поправить селекторы в `cart.css`, это не критично для работы корзины.
- `is_cart()` заведено в `amis_page_builder_layout()`/`amis_no_sidebar_layout()`
  (`inc/enqueue.php`), иначе Astra добавляет свой контейнер/сайдбар поверх
  (см. «Грабли с Astra» выше) — тот же приём, что и у архива каталога.
- Кросс-продажи (`.cross-sells`, блок WooCommerce «С этим товаром покупают»)
  оформлены заранее, но сейчас пустые — это штатное поле Upsells/Cross-sells
  WooCommerce на товаре, отдельное от `_amis_related_probes`/`_amis_related_acc`
  (inc/product-accessories.php). Пока не заполнено ни у одного товара.
- **На момент вёрстки этих правок был включён модуль Astra Pro для
  WooCommerce** («липкая сумма корзины») — **пользователь позже его
  выключил**, так что часть находок ниже описывает поведение, которого
  сейчас уже нет на сайте, но CSS-костыли под него оставлены (они не
  мешают, если элемента, под который написаны, больше нет в DOM — просто
  не находят цель). Если корзина в новой вёрстке снова поедет — первым
  делом проверить, не ищем ли мы решение для уже неактуальной причины.
  Модуль сам оборачивал содержимое корзины в `#ast-cart-wrapper` (между
  `.cart-page` и таблицей/итогом) и держал на нём собственные
  `display`/`flex-direction`/`gap` — даже с `!important` вторая колонка не
  вставала на своё место, потому что специфичность была не при чём: Astra
  Pro, судя по всему, тоже использовал `!important` на этом ID-селекторе.
  Рабочее решение было — не разводить колонки, а снять flex-контекст
  целиком (`display:block !important` на `#ast-cart-wrapper.flex`).
- Ещё один источник «блок не тянется» — не Astra, а сам **WooCommerce
  core** (`woocommerce.css`): `.woocommerce .cart-collaterals .cart_totals,
  .woocommerce-page .cart-collaterals .cart_totals{float:right;width:48%}`.
  Три класса — специфичность выше нашего `body.woocommerce-cart
  .cart_totals`, снимается только через `float:none !important;
  width:100% !important`.

## Текущее состояние страницы «Оформление заказа»

- Тот же приём, что и у корзины: шаблон WooCommerce не переопределяем,
  оборачиваем через хуки `woocommerce_before_checkout_form` /
  `woocommerce_after_checkout_form` (`inc/checkout.php`) — там классический
  шорткод `[woocommerce_checkout]` (не блочный чекаут, проверено в
  редакторе страницы). Оформление — `assets/css/checkout.css`, по
  стабильным классам WooCommerce (`.form-row`, `#order_review`,
  `.wc_payment_methods`, `#place_order` и так далее).
- **Заголовки «Customer information»/«Payment» на английском, которые были
  на первом скриншоте пользователя — это был тот самый модуль Astra Pro
  для WooCommerce** (см. пункт про корзину выше), сейчас выключен. После
  выключения чекаут вернулся к обычным строкам WooCommerce («Платёжные
  реквизиты», «Ваш заказ»). Первая версия `checkout.css` (написанная,
  пока модуль ещё стоял в подозреваемых, но не был подтверждён) на всякий
  случай сразу сделала один столбец на всю страницу — это оказалось
  неверно и только всё сломало: раскладка формы (слева) и «Вашего заказа»
  (справа) здесь — **родной двухколоночный float-layout самого
  WooCommerce/Astra**, никак не связанный с Astra Pro, что подтвердил
  пользователь, найдя в инспекторе штатное правило
  `#customer_details.col2-set{width:55%;float:left;margin-right:4.35%}`
  (`@media min-width:922px`). Разводить их по колонкам самим не нужно —
  это уже работает из коробки; единственная реальная ошибка была в
  `.checkout-page{max-width:640px}`, которое душило обе колонки сразу.
  Второй заход: убрали ограничение ширины, `#order_review` оформлен одной
  карточкой (таблица заказа + оплата внутри неё, с разделителем, а не
  вложенным блоком), пары `.form-row-first`/`.form-row-last` — свой
  float-based CSS вместо более ранней ошибочной попытки через
  `display:grid` (грид на потомке, который родитель уже флоатит, ведёт
  себя непредсказуемо — отсюда и было «криво» в первом заходе).
- Блок «Payment» с текстом «К сожалению, для вас не нашлось подходящих
  способов оплаты» — это штатное уведомление WooCommerce при выключенных
  платёжных методах, не баг вёрстки. Включается в WooCommerce → Настройки
  → Оплата (хотя бы один способ, например «Прямой банковский перевод»
  или «Оплата при получении»).
- `is_checkout()` заведено в `amis_enqueue_assets()`,
  `amis_page_builder_layout()` и `amis_no_sidebar_layout()` (`inc/enqueue.php`)
  — тот же приём, что и у корзины/архива каталога, иначе Astra добавляет
  свой контейнер/сайдбар поверх (см. «Грабли с Astra» выше). Заголовок
  Astra отключён фильтром `astra_the_title_enabled` в `inc/checkout.php`,
  кроме страницы «Заказ получен» (`is_order_received_page()`) — для неё
  отдельного оформления пока нет, дефолтный заголовок Astra там оставлен
  осознанно, чтобы страница не осталась без `<h1>` вовсе.
- **Форма сознательно короче стандартной**: адрес (`billing_address_1/2`,
  `billing_city`, `billing_state`, `billing_postcode`), страна
  (`billing_country`), фамилия (`billing_last_name`) и «Примечание к
  заказу» убраны фильтрами `woocommerce_checkout_fields` /
  `woocommerce_enable_order_notes_field` в `inc/checkout.php` — с сайта
  уходит не готовый заказ с доставкой, а заявка на КП, остальное
  менеджер уточняет сам при обработке. Остались только Имя (растянуто
  на всю строку через `class => form-row-wide`, раз пары с фамилией
  больше нет), Название компании, Телефон, Email.
  Страна всё равно нужна заказу (для WooCommerce, не для формы) —
  подставляется программно как `RU` через фильтр
  `woocommerce_checkout_posted_data`, без участия покупателя. Сайт пока
  работает только по России; если появятся заказы из других стран,
  это место придётся вернуть в форму или сделать выбор менеджером
  вручную в заказе.
  Если модель продаж изменится (появится автоматическая доставка/оплата
  через сайт без участия менеджера) — все эти поля нужно будет вернуть.

## Текущее состояние каталога товаров: категории, фильтры, импорт

### Новые категории каталога

К уже бывшим (`oscilloscopes`, `spectrum-analyzers`, `signal-generators`,
`power-amplifiers`, `power-supplies`, `multimeters`, `probes`) добавились три —
слаги заданы явно при создании (иначе WordPress сгенерировал бы из кириллицы
нечитаемую URL-кодированную строку):
- **Анализаторы цепей** → `network-analyzers` (векторные анализаторы цепей, DNA*).
- **Модульные системы** → `modular-systems` (мейнфреймы M300/M301/M302 и модули).
- **Электронные нагрузки** → `electronic-loads` (DL30*/DL30*A).

Для всех трёх заданы описание категории и характеристика (`amis_spec_label`/
`amis_spec_value`) для карточки на главной, как у остальных.
Карты характеристик (`inc/product-map.php`) для этих трёх категорий **пока не
заведены** — блок «Технические характеристики» на страницах их товаров не
появится, пока не добавить туда записи по образцу `oscilloscopes`/`probes`.

Категория «Пробники» переработана: атрибуты сокращены до подтверждённых —
`bandwidth`, `probe-type` (новый, слаг), `connector`, `compatible-series`
(`inc/product-map.php`). Термин атрибута `probe-type` для всех текущих
моделей — «Активный дифференциальный».

### Бренд и серия

- **Бренд** — штатная таксономия WooCommerce `product_brand` (Товары → Бренды).
- **Серия** — атрибут `pa_series` (Товары → Атрибуты, слаг `series`). Если у
  атрибута включить «Использовать для архивов» — WooCommerce сам создаёт
  страницу по каждому значению серии, она рендерится через тот же
  `woocommerce/archive-product.php`, что и категории (это учтено:
  `is_product_taxonomy()`, а не только `is_tax(['product_cat','product_tag'])`,
  используется и в `$queried_term`, и в `wpseo_canonical`/`wp_robots`-фильтрах).
- На карточке товара (`woocommerce/single-product.php`) бренд и серия — в
  хлебных крошках. Бренд ссылается на свой архив всегда (штатная таксономия
  его гарантирует); серия ссылается на архив только если он реально включён
  (проверка через `is_taxonomy_viewable('pa_series')`) — иначе просто текст.

### Фильтры на архиве каталога (/shop/, категории, метки)

`woocommerce/archive-product.php` + `inc/queries.php`:
- Кроме фильтра «В наличии» (`?instock=1`, было раньше) добавлены **«Бренд»**
  (`?brand=slug`) и **«Серия»** (`?series=slug`) — обычные ссылки с
  GET-параметрами, без AJAX, правят основной запрос через `pre_get_posts`
  (`amis_shop_facet_filter`). Работают одновременно, не сбрасывая друг друга
  (`$with_filters`/`$build_facet_url` в шаблоне).
- Список значений для пилюль бренда/серии — только те, что реально есть у
  товаров **текущей** категории (`amis_get_archive_facet_terms()`), не по
  всему сайту. Показываются только когда в категории больше одного значения.
- Показываются только на `/shop/` и страницах категорий/меток — на страницах
  самого бренда/серии эти же фильтры не рисуются (увели бы с текущего
  выбора, а не дополняли его) — та же логика, что у пилюль категорий
  (`$show_cat_filter`/`$show_facets`).
- Визуально «Бренд»/«Серия» — в отдельной панели с фоном (`.shop-facets` в
  `assets/css/shop.css`), не сливаются с категориями сверху.
- Ссылка **«Сбросить все фильтры»** (`.shop-reset`) появляется, только когда
  активен хотя бы один из instock/brand/series; категорию не трогает.

### SEO: пагинация и фильтры каталога не должны попадать в выдачу

`inc/enqueue.php`:
- `amis_noindex_filtered_shop` (хук `wp_robots`, штатный фильтр WordPress
  с 5.7, независим от Yoast) — ставит `noindex, follow` на `/shop/`/категории
  при пагинации (`/page/2/`) или любом из `?instock=`/`?brand=`/`?series=`.
  Специально `noindex,follow`, а не `robots.txt`: робот должен продолжать
  обходить такие страницы, чтобы доходить по ссылкам до товаров, которые
  видны только на 2-й/3-й странице или под фильтром.
- `amis_canonical_strip_shop_filters` — канонический адрес отфильтрованной
  страницы указывает на неё же без `instock`/`brand`/`series` (пагинацию не
  трогает — у неё canonical должен указывать на саму себя). Навешен и на
  `wpseo_canonical` (сейчас стоит Yoast), и на штатный `get_canonical_url`
  (запасной вариант, если Yoast когда-нибудь отключат — так уже было с
  Astra Pro, см. раздел про корзину/чекаут выше).
- На сайте установлен и активен **Yoast SEO** — заголовки/описания страниц
  правятся в его метабоксе на каждой странице/товаре/категории, глобальные
  шаблоны — SEO → Search Appearance. `robots.txt` редактируется там же
  (SEO → Инструменты → Редактор файлов); согласованный текст обсуждён в
  переписке, но в репозитории не хранится — это конфигурация WordPress,
  не файл темы.

### Массовый импорт и админ-инструменты (все — новые файлы в `inc/`,
### подключены в `functions.php`, пункты меню — под «Товары»)

- **`inc/price-import.php`** («Товары → Обновить цены») — загрузка CSV
  (колонки `sku` + `price`/`regular_price`/«цена», делимитер `,`/`;`/tab
  определяется автоматически, BOM и разделитель-точка-с-запятой у Excel RU
  учтены). Обновляет цену **только** у товаров, совпавших по артикулу — в
  отличие от штатного импортёра WooCommerce, **не создаёт** новых товаров
  для несовпавших строк. Это осознанный выбор: каталог пополняется
  постепенно, часть позиций из прайса поставщика ещё не добавлена на сайт,
  и штатный импортёр в режиме «обновить существующие» всё равно создаёт
  товар, если SKU не находит — здесь это исключено.
- **`inc/accessories-import.php`** («Товары → Импорт аксессуаров») — тоже
  CSV (`sku`/`short_description`/`adapt` (или `compatibility`/`category`)/
  `price`), но задача другая: **создать** новые товары-аксессуары/пробники
  и **привязать** их к приборам через `_amis_related_probes`/
  `_amis_related_acc` (те же поля, что у ручного метабокса,
  `inc/product-accessories.php`).
  - Тип (пробник/аксессуар) определяется по слову «probe» в описании
    (регистронезависимо). Новый товар создаётся **черновиком** — сырые
    данные из прайса поставщика (пустые/повторяющиеся описания у части
    позиций) не должны публиковаться без проверки.
  - Категория новому товару: «Пробники» либо «Аксессуары» (создаётся
    автоматически, если её ещё нет).
  - Если SKU уже существует на сайте — новый товар не создаётся, но
    привязка всё равно выполняется (используется существующий ID).
  - Колонка «с чем совместим» (`adapt`) не содержит точных артикулов
    приборов — это либо название категории («Vector Network Analyzer»),
    либо название серии/семейства («DG5000 Pro»). Разбор —
    **эвристика без гарантии точности**
    (`amis_acc_import_resolve_targets()`):
    1. Похоже на категорию (словарь англ. фраз,
       `amis_acc_import_category_dictionary()`) — берутся все товары
       категории.
    2. Иначе — как префикс артикула: «DG5000» → «DG5» (незначащие нули
       отсекаются), ищутся товары с артикулом на этот префикс. Слово-
       уточнитель в конце («Pro») требуется дополнительно в артикуле/
       названии — иначе «DG5000 Pro» зацепил бы и обычные DG52xx без Pro.
    3. Значения через `/`, `,`, `&» — несколько независимых частей,
       результат объединяется.
    4. Ничего не подошло (`COMMON`, `ALL`, «Accessories and Options» и
       т.п.) — не привязывается, попадает в отчёт как нераспознанное.
  - **Важно, при повторной загрузке (например, после правки CSV)**:
    перед привязкой аксессуар теперь **полностью отвязывается** от всех
    приборов, где стоял раньше (`amis_acc_import_unlink_everywhere()`), а
    потом привязывается заново по текущему файлу — то есть повторная
    загрузка исправленного CSV **пересчитывает** связи с нуля, а не
    наслаивает новые поверх старых неправильных. Это добавили специально
    после того, как первая загрузка привязала часть аксессуаров не туда.
    **Побочный эффект**: если после импорта что-то донастраивали в
    метабоксе руками для этих же аксессуаров — повторная загрузка CSV это
    сотрёт (не различает ручные и импортированные связи). Если ручные
    правки были — предупредить перед повторной загрузкой.
  - Отчёт после загрузки — по каждой строке: создан/уже существовал,
    к каким артикулам приборов привязался, или причина, почему не привязан.
    **Проверить этот отчёт после каждой загрузки** — эвристика не идеальна,
    особенно на составных значениях типа `MHO/DHO5000`, `DS6000 MSO/DS4000`.
- **`inc/rename-import.php`** («Товары → Обновить названия») — CSV
  `sku`/`name`, меняет только название товара по совпавшему артикулу
  (несовпавшие — пропускаются, ничего не создаётся). Понадобился, когда
  `inc/accessories-import.php` создал ~280 товаров с названием прямо из
  английского `short_description` прайса (переводить забыли до загрузки).
- **`inc/slug-fix.php`** («Товары → Пересчитать URL») — без файла, одна
  кнопка: проходит по **всем** товарам сайта и ставит `slug` (адрес)
  равным `sanitize_title(SKU)`. Понадобился, потому что штатный CSV-
  импортёр WooCommerce строил slug из `name`, а `name` был на русском —
  получались смешанные кириллица+латиница адреса вида
  `осциллограф-rigol-dho1104` вместо желаемого `dho1104`. Смена slug у уже
  опубликованного товара не «ломает» старые ссылки — WordPress сам
  сохраняет старый slug (`_wp_old_slug`) и настраивает автоматический
  редирект на новый, штатным механизмом `wp_old_slug_redirect()`.

### Что осталось сделать (на момент этой заметки, не подтверждено выполненным)

- **Перевести названия аксессуаров**: файл `rename_ready.csv` (в корне
  темы) собран и переведён полностью (~280 строк) под `inc/rename-import.php`
  — проверить, залит ли он уже; если нет, залить.
- **Пересчитать slug по всем товарам** через «Товары → Пересчитать URL»
  (`inc/slug-fix.php`) — проверить, запускалось ли; если нет, запустить.
- **Массово выставить реальный «Статус запасов»**: после первого CSV-
  импорта (`woo_import_ready.csv`) все товары получили статус «В наличии»
  по умолчанию, что не соответствует действительности (почти всё под
  заказ). Рекомендованный путь — выделить все товары в админке и через
  «Действия» → «Изменить» → массовое редактирование проставить «Под
  заказ»/«Нет на складе» всем, потом вручную отметить «В наличии» только
  реально доступные (их меньше). Не подтверждено, что это уже сделано.
- **Проверить отчёт `inc/accessories-import.php`** после последней
  загрузки — пользователь отметил, что часть привязок аксессуаров к
  приборам получилась неверной; предполагается вручную поправить
  `woo_acc_mport.csv` (в корне темы) и залить повторно (теперь это
  безопасно — см. «важно» выше про полную переотвязку).
- Временные файлы в корне темы, не являющиеся частью кода сайта — можно
  удалить после использования, если импорт больше не понадобится:
  `woo_import_ready.csv`, `woo_acc_mport.csv` (если пользователь положил
  его туда), `rename_ready.csv`.

### Прочие точечные правки каталога/товара за это же обсуждение

- **Мега-меню каталога в шапке** (`header.php`, `.cat-drop`/`.cat-panel` в
  `assets/css/base.css`) — при наведении на кнопку «Каталог» на десктопе
  раскрывается панель с топ-категориями (`amis_get_top_categories()`) и
  ссылкой «Все категории»; на мобильном (≤1000px) панель скрыта, кнопка —
  обычная ссылка на каталог.
- Категория «Misc» (служебная, для товаров без темы) исключена из
  отображения везде: `amis_get_top_categories()` (`inc/queries.php`),
  фильтр «Базы знаний» (`templates/template-articles.php`), карточка
  статьи (`template-parts/article-card.php`), `single.php`, хлебные крошки
  товара (`woocommerce/single-product.php`).
- **Карточка товара в каталоге** (`template-parts/product-card.php`):
  убрано дублирование «Под заказ» — раньше оно показывалось и лентой в
  углу карточки, и отдельной строкой наличия снизу одновременно. Нижняя
  строка теперь скрывается **только** когда лента и так говорит «Под
  заказ» (товар не featured и не в наличии); если товар «Хит продаж» и
  при этом под заказ — нижняя строка остаётся, это единственное место,
  где видно наличие.
- **Страница товара** (`woocommerce/single-product.php`): убран штатный
  дубль блока наличия — `woocommerce_template_single_add_to_cart()`
  сама выводит `<p class="stock out-of-stock">` («Нет на складе»)
  рядом с нашим собственным блоком (у которого уже «Под заказ» +
  срок поставки из `_amis_lead_time`). Отключено фильтром
  `add_filter('woocommerce_get_stock_html', '__return_empty_string')`
  в `inc/queries.php`.
- Уведомления WooCommerce («Добавлено в корзину» и т.п., `.woocommerce-
  message`/`.woocommerce-error`/`.woocommerce-info`) оформлены глобально
  в `assets/css/base.css`, а не по конкретной странице — WooCommerce
  печатает их в разных местах, иногда раньше собственных обёрток страницы
  (например, на `/cart/` — до хука `woocommerce_before_cart`, из-за чего
  более специфичные правила в `cart.css` их не видят). Кнопка «Просмотр
  корзины» внутри уведомления получила стиль `.btn-dark`-подобный.
